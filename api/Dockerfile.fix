# Thin patch on top of the pre-built Piston image.
# Only replaces the two files with WSL2 fixes, leaving entrypoint untouched.
FROM ghcr.io/engineer-man/piston
COPY src/package.js /piston_api/src/package.js
COPY src/api/v2.js  /piston_api/src/api/v2.js
