#!/usr/bin/env bash

# stdbuf -o0: force unbuffered stdout so printf() prompts appear before scanf() blocks.
# Piston pipes stdin/stdout (not a PTY), so glibc uses full buffering by default.
shift # Discard main filename
stdbuf -o0 ./a.out "$@"
